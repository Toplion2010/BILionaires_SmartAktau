# 💬 Система комментариев SmartAktau

## Обзор

Система комментариев превращает обращения граждан в двусторонний диалог между жителями и городскими службами.

## ✅ Что реализовано

### 1. База данных

**Таблица `comments`:**
- `id` — уникальный идентификатор
- `complaint_id` — связь с обращением
- `author` — имя автора комментария
- `author_role` — роль (user, admin, service)
- `text` — текст комментария
- `created_at` — дата создания
- `updated_at` — дата обновления

**Функции:**
- Автоматический подсчет комментариев
- Триггер для обновления `comments_count` в таблице complaints
- RLS политики для безопасности

### 2. API функции

**`API.getComments(complaintId)`** — получить комментарии для жалобы
**`API.createComment(complaintId, author, text, role)`** — создать комментарий
**`API.updateComment(id, text)`** — обновить комментарий
**`API.deleteComment(id)`** — удалить комментарий

**Fallback:** Все функции работают с localStorage, если Supabase недоступен.

### 3. UI компоненты

**Модальное окно деталей:**
- Полная информация об обращении
- Список всех комментариев
- Форма добавления комментария
- Визуальное разделение по ролям

**Карточки обращений:**
- Счетчик комментариев (💬 N)
- Клик открывает детали
- Hover эффекты

### 4. Роли пользователей

**🔵 Житель (user):**
- Оставляет комментарии
- Задает вопросы
- Добавляет детали к проблеме

**🟢 Администрация (admin):**
- Отвечает на вопросы
- Объясняет решения
- Запрашивает доп. информацию

**🔷 Служба (service):**
- Системные уведомления
- Автоматические статусы

## Настройка в Supabase

### Шаг 1: Создать таблицу

Выполните SQL из файла `comments-schema.sql` в SQL Editor:

```bash
1. Supabase Dashboard → SQL Editor
2. Скопировать comments-schema.sql
3. Выполнить
4. Проверить: Table Editor → comments
```

### Шаг 2: Проверить RLS политики

```sql
-- Должны быть активны:
- "Allow public read access" (SELECT)
- "Allow public insert access" (INSERT)
- "Allow public update access" (UPDATE)
- "Allow service role delete access" (DELETE)
```

### Шаг 3: Проверить триггер

```sql
-- В Table Editor → complaints проверьте наличие поля:
comments_count INTEGER DEFAULT 0
```

## Как использовать

### Для жителей (index.html)

1. **Открыть детали обращения:**
   - Кликнуть на карточку жалобы в ленте
   - ИЛИ кликнуть на маркер на карте → ссылка в popup

2. **Добавить комментарий:**
   - Ввести имя
   - Написать комментарий
   - Нажать "Добавить комментарий"

3. **Просмотреть ответы:**
   - Комментарии администрации выделены зеленым
   - Системные уведомления — синим

### Для администрации (admin.html)

1. **Открыть детали:**
   - В таблице жалоб нажать "Подробнее"
   - Откроется index.html с деталями

2. **Ответить жителю:**
   - Ввести имя (например: "Служба водоснабжения")
   - Написать ответ
   - Комментарий автоматически будет помечен как от администрации

**Примеры ответов:**

```
✅ "Проблема принята в работу. Бригада выедет в течение 24 часов."

❓ "Уточните, пожалуйста, точный адрес и подъезд"

✔️ "Проблема решена 15.11.2025. Спасибо за обращение!"
```

## Примеры сценариев

### Сценарий 1: Уточнение информации

```
👤 Жительница: "Нет воды в 15 микрорайоне"
   [Создает жалобу]

💬 Администрация: "Уточните, пожалуйста, дом и подъезд"

👤 Жительница: "Дом 45, подъезды 2 и 3"

💬 Администрация: "Спасибо! Отправили бригаду"
```

### Сценарий 2: Обновление статуса

```
👤 Житель: "Сломан фонарь на ул. Сатпаева"
   [Создает жалобу]

💬 Служба: "Принято в работу"
   [Меняет статус на "В обработке"]

💬 Служба: "Фонарь отремонтирован 16.11.2025"
   [Меняет статус на "Выполнено"]
```

### Сценарий 3: Разъяснение

```
👤 Жительница: "Почему не вывозят мусор 3 дня?"

💬 Администрация: "Из-за погодных условий был задержан график вывоза. Завтра все будет нормализовано. Приносим извинения!"
```

## Технические детали

### Структура комментария

```javascript
{
  id: 123,
  complaint_id: 456,
  author: "Айгуль Каримова",
  author_role: "user",
  text: "Спасибо за быстрый ответ!",
  created_at: "2025-11-09T12:34:56Z",
  updated_at: "2025-11-09T12:34:56Z"
}
```

### Визуальное отличие по ролям

**CSS классы:**
```css
.comment.user   /* Обычный житель - фиолетовая граница */
.comment.admin  /* Администрация - зеленая граница */
.comment.service /* Служба - синяя граница */
```

**Бейджи:**
```html
<span class="author-badge user">Житель</span>
<span class="author-badge admin">Администрация</span>
<span class="author-badge service">Служба</span>
```

### API Endpoints (Supabase)

```javascript
// GET comments
GET /rest/v1/comments?complaint_id=eq.{id}&order=created_at.asc

// POST new comment
POST /rest/v1/comments
Body: { complaint_id, author, author_role, text }

// PATCH update comment
PATCH /rest/v1/comments?id=eq.{id}
Body: { text }

// DELETE comment
DELETE /rest/v1/comments?id=eq.{id}
```

## Расширение функционала

### 1. Уведомления по email

```javascript
// При создании комментария от админа
async createComment(complaintId, author, text, role) {
    const comment = await API.createComment(...);

    if (role === 'admin') {
        // Отправить email автору жалобы
        await sendEmailNotification(complaintId, comment);
    }
}
```

### 2. Real-time обновления (Supabase Realtime)

```javascript
// Подписка на новые комментарии
const subscription = supabase
    .channel('comments')
    .on('postgres_changes',
        {
            event: 'INSERT',
            schema: 'public',
            table: 'comments',
            filter: `complaint_id=eq.${complaintId}`
        },
        (payload) => {
            // Добавить новый комментарий в UI
            this.addCommentToList(payload.new);
        }
    )
    .subscribe();
```

### 3. Редактирование комментариев

```javascript
// Добавить кнопку "Редактировать" для автора
<button onclick="editComment(${comment.id})">Редактировать</button>

async editComment(id, newText) {
    await API.updateComment(id, newText);
    // Обновить UI
}
```

### 4. Прикрепление файлов

```javascript
// Расширить схему
ALTER TABLE comments ADD COLUMN attachments TEXT[];

// UI: добавить input type="file"
<input type="file" id="commentAttachment">
```

## Тестирование

### Тестовые данные

```sql
-- Добавить тестовые комментарии
INSERT INTO comments (complaint_id, author, author_role, text) VALUES
(1, 'Айгуль Каримова', 'user', 'Спасибо за оперативный ответ!'),
(1, 'Служба водоснабжения', 'admin', 'Проблема решена. Всего доброго!'),
(2, 'Ержан Нурланов', 'user', 'Когда починят?'),
(2, 'Городская администрация', 'admin', 'Ремонтная бригада выедет завтра утром');
```

### Проверочный список

- [ ] Создать жалобу
- [ ] Открыть детали жалобы
- [ ] Добавить комментарий как житель
- [ ] Проверить отображение комментария
- [ ] Открыть admin.html
- [ ] Нажать "Подробнее" на жалобе
- [ ] Добавить ответ как администрация
- [ ] Проверить визуальное отличие (зеленая граница)
- [ ] Проверить счетчик комментариев в ленте
- [ ] Проверить работу без интернета (localStorage)

## Безопасность

### Для демо/хакатона:

✅ Публичный доступ к чтению/записи
✅ RLS включен
✅ Валидация на клиенте

### Для продакшена:

❗ Ограничить INSERT только для authenticated
❗ Добавить проверку автора при UPDATE
❗ Модерация комментариев
❗ Rate limiting (макс. N комментариев/минута)
❗ Фильтр нецензурных слов
❗ Капча при создании комментария

```sql
-- Production RLS policy
CREATE POLICY "Authenticated users can comment"
ON comments FOR INSERT
TO authenticated
WITH CHECK (auth.uid() IS NOT NULL);

CREATE POLICY "Users can edit own comments"
ON comments FOR UPDATE
TO authenticated
USING (auth.uid() = user_id) -- Нужно добавить user_id
WITH CHECK (auth.uid() = user_id);
```

## Метрики (для аналитики)

**Полезные показатели:**

- Среднее количество комментариев на жалобу
- Процент жалоб с ответами администрации
- Среднее время ответа
- Самые активные диалоги

```sql
-- Средние комментарии на жалобу
SELECT AVG(comments_count) FROM complaints;

-- Процент с ответами
SELECT
    COUNT(DISTINCT complaint_id) * 100.0 / (SELECT COUNT(*) FROM complaints)
FROM comments
WHERE author_role = 'admin';
```

---

**Система комментариев готова!** 💬

Теперь SmartAktau — это не просто форма жалоб, а полноценная платформа для диалога между горожанами и администрацией.
