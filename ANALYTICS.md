# 📊 Аналитика SmartAktau

## Обзор

Страница аналитики (`analytics.html`) предоставляет визуализацию данных о городских обращениях с помощью интерактивных диаграмм и графиков.

## Что включено

### 1. Статистические карточки

**4 основных показателя:**
- 📋 **Всего обращений** — общее количество
- 🆕 **Новые** — обращения со статусом "Получено"
- ⚙️ **В обработке** — активные обращения
- ✅ **Выполнено** — решенные проблемы

### 2. Диаграммы

#### 📌 Распределение по категориям (Doughnut Chart)
- Показывает процентное соотношение обращений по категориям
- Категории: Водоснабжение, Мусор, Освещение, Дороги, Другое
- Цветовая кодировка для быстрого восприятия

#### 📊 Распределение по статусам (Bar Chart)
- Столбчатая диаграмма статусов обращений
- Статусы: Получено, В обработке, Выполнено
- Показывает эффективность работы служб

#### 📈 Динамика обращений (Line Chart)
- Временной график поступления обращений
- Показывает тренды и всплески активности
- Помогает планировать ресурсы

### 3. Фильтр по периоду

Выбор временного диапазона:
- **7 дней** — последняя неделя
- **30 дней** — последний месяц (по умолчанию)
- **90 дней** — последние 3 месяца
- **Всё время** — все данные

### 4. Ключевые выводы

Автоматически генерируемые инсайты:
- **Самая частая категория** — топ проблем города
- **Показатель решения** — процент выполненных обращений
- **Среднее время обработки** — эффективность работы
- **Период активности** — общая статистика

## Технические детали

### Используемые библиотеки

- **Chart.js 4.4.0** — современная библиотека для создания графиков
- **Supabase Client** — загрузка данных из БД
- **ES6 Modules** — модульная архитектура

### Структура данных

Аналитика работает с данными из таблицы `complaints`:

```javascript
{
  id: bigint,
  public_id: string,
  title: string,
  description: string,
  category: 'water' | 'garbage' | 'lighting' | 'roads' | 'other',
  status: 'Получено' | 'В обработке' | 'Выполнено',
  lat: number,
  lon: number,
  created_at: timestamp,
  updated_at: timestamp
}
```

### Функции обработки данных

#### `groupByCategory(complaints)`
Группирует обращения по категориям для круговой диаграммы.

```javascript
{
  water: 15,
  garbage: 8,
  lighting: 5,
  roads: 12,
  other: 3
}
```

#### `groupByStatus(complaints)`
Группирует обращения по статусам для столбчатой диаграммы.

```javascript
{
  'Получено': 10,
  'В обработке': 15,
  'Выполнено': 18
}
```

#### `groupByDate(complaints)`
Группирует обращения по датам для линейного графика.

```javascript
{
  '2025-11-01': 3,
  '2025-11-02': 5,
  '2025-11-03': 2,
  // ...
}
```

### Обновление данных

```javascript
// Автоматическое обновление при загрузке
Analytics.init();

// Ручное обновление по кнопке "Обновить"
document.getElementById('refreshBtn').click();

// Изменение периода
document.getElementById('periodFilter').value = '7'; // 7 дней
```

## Как использовать

### Доступ к странице

**3 способа:**

1. **С главной страницы:**
   - index.html → кнопка "📊 Аналитика"

2. **Из админ-панели:**
   - admin.html → кнопка "📊 Аналитика"

3. **Прямая ссылка:**
   - http://localhost:8000/analytics.html

### Интерактивность

**Диаграммы:**
- Наведите курсор на элемент → увидите подробности
- Проценты и точные значения в tooltip

**Фильтр:**
- Выберите период в dropdown → графики обновятся автоматически

**Обновление:**
- Нажмите "🔄 Обновить" → загрузит последние данные из Supabase

## Примеры использования

### Для администрации

**Выявление проблемных зон:**
```
1. Открыть analytics.html
2. Посмотреть "Распределение по категориям"
3. Топ категория = приоритетное направление
```

**Оценка эффективности:**
```
1. Проверить "Показатель решения"
2. Если < 70% → нужно усилить работу служб
3. Если > 90% → отличный результат!
```

**Планирование ресурсов:**
```
1. Изучить "Динамику обращений"
2. Всплеск в определенные дни = выделить больше сотрудников
3. Спад = оптимизировать график работы
```

### Для демо/презентации

**Впечатляющие метрики:**

1. **Общая статистика:**
   - "За месяц обработано X обращений"
   - "Показатель решения Y%"

2. **Тренды:**
   - "Рост обращений на Z% за неделю"
   - "Самая популярная категория — Водоснабжение"

3. **Эффективность:**
   - "Среднее время обработки — 2-3 дня"
   - "Выполнено 90% обращений"

## Расширение функционала

### Добавление новых графиков

**Пример: Heatmap по районам**

```javascript
// В analytics.js
updateHeatmapChart(complaints) {
    // Группировка по координатам
    const heatmapData = this.groupByLocation(complaints);

    // Создание heatmap с Leaflet.heat
    // ...
}
```

**Пример: Pie chart по времени суток**

```javascript
groupByTimeOfDay(complaints) {
    const grouped = {
        'Утро (6-12)': 0,
        'День (12-18)': 0,
        'Вечер (18-24)': 0,
        'Ночь (0-6)': 0
    };

    complaints.forEach(c => {
        const hour = new Date(c.created_at).getHours();
        if (hour >= 6 && hour < 12) grouped['Утро (6-12)']++;
        else if (hour >= 12 && hour < 18) grouped['День (12-18)']++;
        else if (hour >= 18 && hour < 24) grouped['Вечер (18-24)']++;
        else grouped['Ночь (0-6)']++;
    });

    return grouped;
}
```

### Экспорт данных

**Добавить кнопку экспорта в PDF/Excel:**

```javascript
// analytics.js
exportToPDF() {
    // Используя jsPDF
    const pdf = new jsPDF();
    pdf.text('SmartAktau Analytics', 10, 10);
    // Добавить графики как изображения
    // ...
    pdf.save('smartaktau-analytics.pdf');
}
```

## Советы для демо

### Подготовка данных

**Создайте разнообразные тестовые данные:**

```javascript
// В консоли браузера
const categories = ['water', 'garbage', 'lighting', 'roads', 'other'];
const statuses = ['Получено', 'В обработке', 'Выполнено'];

for (let i = 0; i < 50; i++) {
    const category = categories[Math.floor(Math.random() * categories.length)];
    const status = statuses[Math.floor(Math.random() * statuses.length)];

    // Создать жалобу через форму
    // Или прямо в Supabase
}
```

### Лучшие практики

1. **Минимум 20-30 обращений** — для красивых графиков
2. **Разные категории** — покажет разнообразие проблем
3. **Разные статусы** — продемонстрирует работу системы
4. **Данные за несколько дней** — для временного графика

### Сценарий демо

```
1. Открыть главную страницу
   → "Вот наша платформа для жителей"

2. Создать 1-2 жалобы
   → "Процесс подачи простой и быстрый"

3. Перейти в Аналитику
   → "А вот как администрация видит данные"

4. Показать диаграммы
   → "Видим, что больше всего проблем с [категория]"

5. Изменить фильтр периода
   → "Можно анализировать любой временной отрезок"

6. Показать ключевые выводы
   → "Система автоматически генерирует инсайты"

7. Перейти в Админ-панель
   → "И вот как службы управляют обращениями"
```

## Производительность

**Оптимизация для больших данных:**

- Пагинация при > 1000 записей
- Lazy loading для графиков
- Кэширование обработанных данных

```javascript
// Пример с кэшированием
let cachedData = null;
let cacheTime = null;

async loadData() {
    const now = Date.now();
    if (cachedData && (now - cacheTime < 60000)) {
        // Использовать кэш (1 минута)
        return cachedData;
    }

    cachedData = await API.getComplaints();
    cacheTime = now;
    return cachedData;
}
```

---

**Аналитика готова к демо!** 📊🚀

Профессиональная визуализация данных покажет ценность SmartAktau для принятия решений.
