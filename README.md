# 🏙️ SmartAktau — Город, который слышит жителей

Веб-платформа для жителей Актау для оперативного сообщения о городских проблемах и отслеживания их решения.

## ✅ Что готово

- ✅ Адаптивная верстка (header, карта, форма, лента)
- ✅ Интерактивная карта Актау на Leaflet
- ✅ Форма создания жалоб с категориями
- ✅ Выбор координат на карте при клике
- ✅ **Интеграция с Supabase** (PostgreSQL + Storage)
- ✅ Загрузка фото в Supabase Storage
- ✅ Отображение жалоб в ленте и на карте
- ✅ Статусы обращений (Получено, В обработке, Выполнено)
- ✅ **Админ-панель** для управления жалобами
- ✅ **Страница аналитики** с диаграммами и графиками (Chart.js)
- ✅ **Система комментариев** — двусторонняя коммуникация
- ✅ Fallback на localStorage (работает офлайн)
- ✅ Счетчик символов в описании

## 🚀 Быстрый старт

### 1. Клонировать репозиторий
```bash
git clone <your-repo-url>
cd SmartAktau
```

### 2. Настроить Supabase

#### 2.1 Создать проект в Supabase
1. Перейти на [supabase.com](https://supabase.com)
2. Создать новый проект
3. Скопировать `URL` и `anon key`

#### 2.2 Создать таблицу
1. Открыть SQL Editor в Supabase Dashboard
2. Скопировать содержимое файла `schema.sql`
3. Выполнить SQL запрос

#### 2.3 Создать Storage bucket
1. Перейти в Storage → Create bucket
2. Имя: `uploads`
3. Public: ✅ Yes (для демо)
4. Следовать инструкциям в `storage-setup.md`

### 3. Настроить конфигурацию

Скопировать `config.example.js` → `config.js`:

```bash
cp config.example.js config.js
```

Отредактировать `config.js` с реальными ключами:

```javascript
export const SUPABASE_URL = 'https://your-project.supabase.co'
export const SUPABASE_KEY = 'your-anon-key-here'
```

**⚠️ ВАЖНО:** Файл `config.js` добавлен в `.gitignore` и НЕ должен коммититься!

### 4. Запустить локально

Просто открыть `index.html` в браузере:

```bash
open index.html
# или
python3 -m http.server 8000
# затем открыть http://localhost:8000
```

### 5. Админ-панель

Открыть `admin.html` для управления жалобами:
- **Пароль по умолчанию:** `admin123`
- Изменить статусы жалоб
- Просмотр статистики

### 6. Аналитика

Открыть `analytics.html` для просмотра дашборда:
- **Диаграммы** по категориям и статусам
- **Временной график** динамики обращений
- **Ключевые выводы** и статистика
- **Фильтр по периоду** (7/30/90 дней, всё время)

## 📁 Структура проекта

```
SmartAktau/
├── index.html              # Главная страница (пользователи)
├── admin.html              # Админ-панель
├── analytics.html          # Страница аналитики с графиками
├── config.js               # Конфигурация Supabase (НЕ коммитить!)
├── config.example.js       # Пример конфигурации (коммитить)
├── .gitignore              # Исключения для git
├── schema.sql              # SQL схема для Supabase
├── storage-setup.md        # Инструкции для Storage
├── css/
│   └── style.css           # Все стили
├── js/
│   ├── api.js              # Supabase API + localStorage fallback
│   ├── map.js              # Leaflet карта
│   ├── app.js              # Логика главной страницы
│   ├── admin.js            # Логика админ-панели
│   └── analytics.js        # Логика аналитики (Chart.js)
└── README.md               # Этот файл
```

## 🔧 Технологии

**Frontend:**
- HTML5/CSS3 — разметка и стилизация
- JavaScript (ES6 Modules) — логика приложения
- Leaflet.js — интерактивная карта
- OpenStreetMap — карты
- Chart.js — графики и диаграммы для аналитики

**Backend:**
- Supabase — PostgreSQL база данных
- Supabase Storage — хранение фото
- localStorage — офлайн fallback

## 🔐 Безопасность

### Что НЕ коммитить:
```
❌ config.js
❌ secret_service_role.txt
❌ .env файлы
```

### Используемые ключи:

1. **anon key** (публичный):
   - ✅ Используется на фронтенде
   - ✅ Безопасен для клиента
   - ✅ Ограничен RLS политиками

2. **service_role key** (приватный):
   - ❌ НИКОГДА не использовать в клиентском коде
   - ✅ Только для серверных задач
   - ✅ Хранится в `secret_service_role.txt` (gitignored)

### Row Level Security (RLS)

Таблица `complaints` защищена RLS политиками:
- **SELECT** — разрешено всем (публичный доступ)
- **INSERT** — разрешено всем (пользователи могут создавать жалобы)
- **UPDATE** — разрешено всем (для демо; в проде — только auth)
- **DELETE** — только `service_role`

## 🎯 Функции API (js/api.js)

### Основные функции:

```javascript
// Инициализация Supabase
API.initSupabase()

// Создание жалобы (с загрузкой фото)
await API.createComplaint(payload, photoFile)

// Получение всех жалоб
await API.getComplaints()

// Обновление статуса
await API.updateStatus(id, 'В обработке')

// Валидация фото
API.validateImageFile(file)

// Загрузка фото в Storage
await API.uploadPhoto(file, complaintPublicId)
```

### Fallback на localStorage:

Если Supabase недоступен, все данные автоматически сохраняются в `localStorage`.

## 📸 Загрузка фото

**Формат:** JPEG, PNG, WebP
**Макс. размер:** 10 MB
**Bucket:** `uploads` (публичный)
**Путь:** `uploads/{public_id}/{timestamp}_{filename}`

Пример:
```
uploads/ACT-1731234567890/1731234567890_photo.jpg
```

## 🌍 Деплой

### Вариант 1: Статический хостинг (Vercel, Netlify, GitHub Pages)

1. Создать `vercel.json` или `netlify.toml`
2. Добавить переменные окружения:
   ```
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_KEY=your-anon-key
   ```
3. Деплой:
   ```bash
   vercel deploy
   # или
   netlify deploy
   ```

### Вариант 2: GitHub Pages

1. Создать `config.js` в репозитории (НЕ коммитить в main!)
2. Использовать отдельную ветку для деплоя
3. Настроить GitHub Actions для инъекции ключей

### Вариант 3: Свой сервер

```bash
# Nginx или Apache
# Просто скопировать файлы в /var/www/html
```

## 🔜 Roadmap

- [ ] Казахский язык (двуязычие)
- [ ] Push-уведомления при изменении статуса
- [ ] Telegram-бот для отправки жалоб
- [ ] AI-категоризация жалоб
- [ ] Аналитика и статистика
- [ ] Фильтры по категориям и статусам
- [ ] Поиск по жалобам
- [ ] Экспорт отчетов (PDF, Excel)
- [ ] Supabase Auth для админов
- [ ] Email-уведомления

## 🛡️ После хакатона (Security Checklist)

После демо **обязательно**:

1. ✅ Ротация `service_role` ключа
2. ✅ Изменить пароль админ-панели
3. ✅ Включить Supabase Auth для админов
4. ✅ Сделать Storage bucket приватным (использовать signed URLs)
5. ✅ Ограничить RLS политики (только authenticated могут обновлять)
6. ✅ Добавить rate limiting
7. ✅ Настроить CORS в Supabase

## 📝 Заметки для разработки

### Очистка всех жалоб (тест):
```javascript
// В консоли браузера (F12)
API.clearAll();
location.reload();
```

### Координаты Актау:
- **Широта:** 43.65°N
- **Долгота:** 51.20°E
- **Зум:** 12

### Просмотр данных в Supabase:
```sql
-- В SQL Editor
SELECT * FROM complaints ORDER BY created_at DESC;
```

## 🤝 Контакты и поддержка

Вопросы? Идеи? Предложения?

- **GitHub Issues:** [создать issue](#)
- **Email:** your-email@example.com

---

**SmartAktau** — делаем наш город лучше вместе! 🌟

Made with ❤️ for Aktau
